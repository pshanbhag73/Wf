db.collection.aggregate([
  {
    $addFields: {
      ipOnly: {
        $regexFind: {
          input: "$fieldName", // Replace with your field name
          regex: "^(?:\\[?)(.*?)(?:\\]?:\\d+$|$)" // Matches IPv4 or IPv6 before port
        }
      }
    }
  },
  {
    $group: {
      _id: "$ipOnly.match" // Group by the extracted IP
    }
  },
  {
    $match: {
      "_id": { $ne: null } // Exclude null matches
    }
  },
  {
    $project: {
      _id: 0,
      ip: "$_id"
    }
  }
])
