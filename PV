nodeGroup.append('text')
      .attr('x', 20)
      .attr('y', 40)
      .attr('dy', '0.35em')
      .attr('font-size', 10)
      .attr('text-anchor', 'start')
      .attr('fill', '#666')
      .text(d => d.location || '');
